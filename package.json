{
  "name": "@cycloidal/driver-router",
  "version": "0.2.2",
  "author": "Jakob Hohlfeld",
  "description": "A router for cycle.js using uniloc and history.",
  "license": "MIT",
  "main": "dist/driver-router.js",
  "scripts": {
    "precompile-lib": "rm -rf dist && mkdir -p dist",
    "compile-lib": "babel src/ -d dist/",
    "prepublish": "npm run precompile-lib && npm run compile-lib"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cycloidal/driver-router.git"
  },
  "keywords": [
    "cycle",
    "router",
    "reactive",
    "rxjs",
    "rx"
  ],
  "bugs": {
    "url": "https://github.com/cycloidal/driver-router/issues"
  },
  "homepage": "https://github.com/cycloidal/driver-router#readme",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-1": "^6.5.0"
  },
  "peerDependencies": {
    "history": "^2.1.1",
    "rx": "^4.1.0",
    "uniloc": "^0.3.0"
  },
  "dependencies": {
    "history": "^2.1.1",
    "uniloc": "^0.3.0"
  },
  "files": [
    "dist/"
  ]
}
